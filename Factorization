set<int> divs(int n) {
   set<int> divs;
    divs.emplace_back(1);
    divs.emplace(n);
    for (int i = 2; i * i <= n; i++) {
        if (n % i == 0) {
            divs.emplace_back(i);
            if (n / i != i)divs.emplace(n / i);
        }
    }
    return divs;
}
